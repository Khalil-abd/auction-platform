server:
  port: 8080

app:
  security:
    jwt:
      secret-key: ${JWT_SECRET}

spring:
  application:
    name: gateway-service

management:
  endpoints:
    web:
      exposure:
        include: health